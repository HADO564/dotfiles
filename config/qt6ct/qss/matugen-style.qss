/* =========================================================
   GLOBAL & BASICS 
========================================================= */
QWidget {
    font-family: "JetBrains Mono";
    font-size: 11pt;
    color: #efdfe1;
    /* REMOVED: background-color: transparent; (Fixes Qt5 transparent rendering bugs) */
}

QMainWindow, QDialog {
    background-color: #191113;
}

QToolTip {
    background-color: #3c3235;
    color: #efdfe1;
    border: 1px solid #514347;
    border-radius: 6px;
    padding: 6px;
}

/* =========================================================
   BUTTONS
========================================================= */
QPushButton {
    background-color: #31282a;
    color: #efdfe1;
    border: 1px solid #514347;
    border-radius: 6px;
    padding: 6px 16px;
    font-weight: bold;
}

QPushButton:hover {
    background-color: #3c3235;
    border: 1px solid #9e8c90;
}

QPushButton:pressed, QPushButton:checked {
    background-color: #ffb0c8;
    color: #541d32;
    border: 1px solid #ffb0c8;
}

QPushButton:disabled {
    background-color: #261d20;
    color: #9e8c90;
    border: 1px solid transparent;
}

/* =========================================================
   INPUTS (Spinners, Dropdowns only)
   Note: QLineEdit, QTextEdit, and QPlainTextEdit are intentionally 
   omitted so Telegram's custom chat inputs remain unbroken. 
   qt5ct/qt6ct will still color them correctly via the palette.
========================================================= */
QSpinBox, QDoubleSpinBox, QComboBox {
    background-color: #140c0e;
    color: #efdfe1;
    border: 1px solid #9e8c90;
    border-radius: 6px;
    padding: 6px 10px;
    selection-background-color: #ffb0c8;
    selection-color: #541d32;
}

QSpinBox:focus, QDoubleSpinBox:focus, QComboBox:focus {
    border: 2px solid #ffb0c8;
    padding: 5px 9px; /* Compensate for thicker border */
}

QComboBox::drop-down {
    border: none;
    width: 24px;
}

QComboBox::down-arrow {
    image: none; /* Strip native ugly arrow */
    border-left: 5px solid transparent;
    border-right: 5px solid transparent;
    border-top: 5px solid #efdfe1;
    margin-right: 10px;
}

/* =========================================================
   CHECKBOXES & RADIOS
========================================================= */
QCheckBox, QRadioButton {
    spacing: 8px;
}

QCheckBox::indicator, QRadioButton::indicator {
    width: 18px;
    height: 18px;
    background-color: #140c0e;
    border: 2px solid #9e8c90;
}

QCheckBox::indicator {
    border-radius: 4px;
}

QRadioButton::indicator {
    border-radius: 11px; /* Circular */
}

QCheckBox::indicator:hover, QRadioButton::indicator:hover {
    border: 2px solid #ffb0c8;
}

QCheckBox::indicator:checked, QRadioButton::indicator:checked {
    background-color: #ffb0c8;
    border: 2px solid #ffb0c8;
}

/* Add inner dots/ticks for checked state using borders */
QRadioButton::indicator:checked {
    width: 10px;
    height: 10px;
    border: 6px solid #ffb0c8;
    background-color: #541d32;
}

/* =========================================================
   SLIDERS & PROGRESS BARS
========================================================= */
QSlider::groove:horizontal {
    border-radius: 3px;
    height: 6px;
    background: #3c3235;
}

QSlider::sub-page:horizontal {
    background: #ffb0c8;
    border-radius: 3px;
}

QSlider::handle:horizontal {
    background: #ffb0c8;
    width: 16px;
    height: 16px;
    margin: -5px 0; /* Align handle with groove */
    border-radius: 8px;
}

QSlider::handle:horizontal:hover {
    background: #703349;
    border: 2px solid #ffb0c8;
}

QProgressBar {
    background-color: #3c3235;
    border-radius: 6px;
    text-align: center;
    color: #efdfe1;
}

QProgressBar::chunk {
    background-color: #ffb0c8;
    border-radius: 6px;
}

/* =========================================================
   TABS & GROUP BOXES
========================================================= */
QTabWidget::pane {
    border: 1px solid #514347;
    border-radius: 8px;
    background: #191113;
    top: -1px;
}

QTabBar::tab {
    background: transparent;
    color: #d5c2c6;
    padding: 8px 16px;
    border-bottom: 2px solid transparent;
}

QTabBar::tab:hover {
    color: #efdfe1;
    background: #140c0e;
}

QTabBar::tab:selected {
    color: #ffb0c8;
    border-bottom: 2px solid #ffb0c8;
    font-weight: bold;
}

QGroupBox {
    border: 1px solid #514347;
    border-radius: 8px;
    margin-top: 16px;
    padding-top: 16px;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0 8px;
    color: #ffb0c8;
    font-weight: bold;
}

/* =========================================================
   VIEWS (Lists, Trees, Tables)
========================================================= */
QTreeView, QListView, QTableView {
    background-color: #140c0e;
    alternate-background-color: #191113;
    border: 1px solid #514347;
    border-radius: 6px;
    gridline-color: #31282a;
}

QTreeView::item:selected, QListView::item:selected, QTableView::item:selected {
    background-color: #ffb0c8;
    color: #541d32;
}

/* =========================================================
   MENUS
========================================================= */
QMenuBar {
    background-color: transparent;
}

QMenuBar::item:selected {
    background-color: #31282a;
    border-radius: 4px;
}

QMenu {
    background-color: #31282a;
    border: 1px solid #514347;
    border-radius: 8px;
    padding: 6px;
}

QMenu::item {
    padding: 6px 24px;
    border-radius: 4px;
}

QMenu::item:selected {
    background-color: #ffb0c8;
    color: #541d32;
}

/* =========================================================
   SCROLLBARS
========================================================= */
QScrollBar:vertical {
    border: none;
    background: #261d20;
    width: 12px;
    border-radius: 6px;
}

QScrollBar::handle:vertical {
    background: #514347;
    min-height: 30px;
    border-radius: 6px;
    margin: 2px;
}

QScrollBar::handle:vertical:hover {
    background: #9e8c90;
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    height: 0px; /* Hide arrows */
}
